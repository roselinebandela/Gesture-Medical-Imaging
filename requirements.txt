# Installed and verified together in this project's .venv.
# For a much smaller download of torch/torchvision (no bundled CUDA), install with:
#   pip install --index-url https://download.pytorch.org/whl/cpu torch torchvision
# then: pip install -r requirements.txt
numpy==2.4.6
opencv-python==5.0.0.93
mediapipe==0.10.8
pillow==12.3.0
torch==2.13.0
torchvision==0.28.0
torchxrayvision==1.5.2
requests==2.34.2
